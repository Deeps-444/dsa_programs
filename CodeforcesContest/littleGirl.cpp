#include <iostream>
using namespace std;

#include <algorithm>

int main()
{
    int n, q;
    cin >> n >> q;
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }

    for (int i = 0; i < q; i++)
    {
        int l, r;
        cin >> l >> r;
    }

    arr.sort();
    int count = 0;
    while(count != q){
        int k = l-r + 1;

    }never met someone so stupid imean omg
}