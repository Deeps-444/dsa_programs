#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n;
    cin >> n;
    vector<int> arr = int[n];

    int total_sum = (n * (n + 1)) / 2;
    if (total_sum % 2)
    {
        cout << "yes";
    }
    else
    {
        cout << "no";
    }

    int sum = 0;
    for (int i = n; i > 0; i--)
    {
    }

    return 0;
}